{
  "name": "@app/e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright E2E — 5 골든 패스 (Home·Editor·Article·Delete cascade·Tag filter)",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "test": "playwright test",
    "test:install": "playwright install chromium",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.49.1"
  }
}
