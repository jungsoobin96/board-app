# Conduit Lite — dev profile 환경 변수 예시
# 사용: cp .env.dev.example .env.dev (실 .env.dev는 .gitignore — 절대 커밋 금지)

# Express 부팅
PORT=3000
NODE_ENV=dev
LOG_LEVEL=debug

# DB (이슈 #3 Prisma에서 사용)
DATABASE_URL="file:./backend/prisma/dev.db"
